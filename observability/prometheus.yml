# Prometheus scrape config for the local compose stack.
global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: lsa-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
