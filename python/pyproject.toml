[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lsa"
version = "0.1.0"
description = "Ingestion and API layer of lsa-platform (demonstration project, synthetic data only)"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "typer>=0.12",
    "pydantic>=2.7",
    "psycopg[binary]>=3.2",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.52",
    "jinja2>=3.1",
    "prometheus-client>=0.20",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.16",
    "mypy>=1.10",
    "pytest>=9",
    "pytest-cov>=7",
    "httpx>=0.27",
    "pip-audit>=2.7",
]

[project.scripts]
lsa-ingest = "lsa.ingest.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["lsa"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "PL"]

[tool.ruff.lint.per-file-ignores]
# Tests assert against concrete expected numbers by nature, and defer app
# imports until fixtures have prepared the environment.
"tests/*" = ["PLR2004", "PLC0415"]

[tool.mypy]
strict = true
files = ["lsa"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"

[tool.coverage.run]
source = ["lsa"]
