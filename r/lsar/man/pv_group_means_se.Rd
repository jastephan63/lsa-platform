% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{pv_group_means_se}
\alias{pv_group_means_se}
\title{Weighted group means with full design-based variance}
\usage{
pv_group_means_se(
  data,
  reps,
  rep_weights,
  group,
  pv_cols = paste0("pv", 1:5),
  weight_col = "final_weight",
  conf_level = 0.95
)
}
\arguments{
\item{data}{One row per participating student: the grouping column,
\code{student_id}, the plausible-value columns, and the full-sample weight.}

\item{reps}{Replicate definitions: \code{replicate_id}, \code{jk_factor}.}

\item{rep_weights}{Long format: \code{student_id}, \code{replicate_id}, \code{weight}.}

\item{group}{Name of the grouping column.}

\item{pv_cols}{Plausible-value column names.}

\item{weight_col}{Full-sample weight column.}

\item{conf_level}{Confidence level for the normal-approximation interval.}
}
\value{
A data.frame with \code{group}, \code{n}, \code{estimate}, \code{se}, \code{ci_lower},
\code{ci_upper}, \code{sampling_var}, \code{imputation_var}.
}
\description{
Extends \code{\link[=pv_group_means]{pv_group_means()}} with sampling variance from jackknife
replicate weights (JKn, delete one PSU) combined with imputation
variance across plausible values under Rubin's rules:
\deqn{T = \bar{U} + (1 + 1/M) B}
where \eqn{\bar{U}} is the mean over plausible values of the replication
variance \eqn{U_m = \sum_r f_r (\hat\theta_{mr} - \hat\theta_m)^2}
(with \eqn{f_r} the replicate's jackknife factor) and \eqn{B} the
between-imputation variance of the per-PV estimates.
}
