% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_results}
\alias{publish_results}
\title{Publish inference results to the platform's results store}
\usage{
publish_results(conn, results, group_type)
}
\arguments{
\item{conn}{A \code{DBIConnection} from \code{\link[=lsa_connect]{lsa_connect()}} — must be the analyst
role, the only one granted write access to \code{analysis_result}.}

\item{results}{A data.frame from \code{\link[=pv_group_means_se]{pv_group_means_se()}} +
\code{\link[=suppress_small_cells]{suppress_small_cells()}}: the grouping column first, then \code{n},
\code{estimate}, \code{se}, \code{ci_lower}, \code{ci_upper}, \code{suppressed}.}

\item{group_type}{Either \code{"canton"} or \code{"language_region"}.}
}
\value{
Invisibly, the number of rows published.
}
\description{
Writes the output of \code{\link[=pv_group_means_se]{pv_group_means_se()}} (after
\code{\link[=suppress_small_cells]{suppress_small_cells()}}) into the \code{analysis_result} table, replacing any
previous publication for the same \code{group_type}. This is how standard
errors and confidence intervals reach the API: R computes them, the
database stores them, the API serves them — the jackknife is never
re-implemented outside this package.
}
